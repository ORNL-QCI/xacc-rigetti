add_xacc_test(QVMAccelerator)
if(${XACC_HAS_ANTLR})
    include_directories(${CMAKE_SOURCE_DIR}/compiler)
    add_xacc_test(QuilCompiler)
    target_link_libraries(QuilCompilerTester xacc-rigetti-quilcompiler)
endif()
add_xacc_test(QuilVisitor)
